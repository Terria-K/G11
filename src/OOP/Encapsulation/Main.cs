using System;

class Program 
{
    static void Main(string[] args) 
    {
        Person person = new Person("Kowalski", 14);
        Console.WriteLine(person.name);

        // TODO Fix the errors by making a getter and setters
    }
}